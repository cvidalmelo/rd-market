# Copia este archivo como .env antes de correr el proyecto:
#   cp .env.example .env
DATABASE_URL="file:./dev.db"

# Clave para firmar la cookie de sesion. Genera la tuya con:
#   openssl rand -base64 32
SESSION_SECRET="cambia-esta-clave-por-una-generada"
